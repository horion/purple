version: "3.7"
services:
  purple:
    build:
      context: ./target
      dockerfile: Dockerfile
    entrypoint: [
            "/bin/bash",
            "entrypoint.sh",
            "operations"
    ]
